import React, { Component } from 'react';
import PropTypes from 'prop-types';

class ReactTools extends Component {
    render() {
        return (
            <div className="ReactTools">
                ReactTools
            </div>
        );
    }
}

ReactTools.propTypes = {

};

export default ReactTools;